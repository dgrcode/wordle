/**
 * Global control for the logs to show. Update the logs below to show specific
 * logs throughout the code execution
 */
const logs = {
  debug: false,
  performance: false,
  cache: false,
};

export default logs;
